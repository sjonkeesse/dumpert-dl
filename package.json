{
  "name": "ddl",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "ddl": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "cli",
    "url"
  ],
  "author": "",
  "license": "ISC",
  "description": "A simple CLI tool to print URLs",
  "dependencies": {
    "@renmu/m3u8-downloader": "^0.4.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
